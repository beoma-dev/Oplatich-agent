# ROADMAP — план повышения качества invoice-bot

Актуален на 2026-08-04 (коммит `adb8138`, 103 теста). Текущее состояние и
архитектура — в [CONTEXT.md](CONTEXT.md), модель угроз — в
[SECURITY.md](SECURITY.md).

**Как читать.** Пункты сгруппированы в горизонты по убыванию отношения
«польза / усилие» с учётом рисков. Внутри горизонта порядок — рекомендуемая
последовательность. У каждого пункта: мотивация → объём → критерий готовности
(DoD). Оценки — в человеко-днях для одного разработчика.

---

## Горизонт 1 · Эксплуатационная надёжность (~1–2 дня)

Сейчас бот «молчит, если умер»: падение заметят люди, а не алерт; бэкап —
ручной. Это самый дешёвый способ снять самые дорогие риски.

> **Статус:** горизонт закрыт — 1.1 ✅, 1.2 ✅, 1.3 ✅, 1.4 ✅, 1.5 ✅
> (2026-08-04). 1.4: `/api/health` отдаёт 503, когда Telegram недоступен
> (пульс getMe в `services/health.py`), инструкция по UptimeRobot — в
> DEPLOY.md; сам монитор заводится вручную за 5 минут. 1.5: `PROXY_URL`
> принимает список через запятую, при старте выбирается первый отвечающий
> (`services/proxy.py`, покрыто тестами).

### 1.1 Алерты об ошибках в Telegram (0.5 д)
- **Зачем:** необработанные исключения видны только в `docker logs`.
- **Что:** в `on_error` (main.py) — отправка краткой карточки ошибки админам
  из `ADMIN_IDS` (с троттлингом: не чаще N в час, схлопывание повторов);
  критичные события аудита (REQUEST_FAILED) — туда же.
- **DoD:** искусственная ошибка в хендлере приходит админу в личку ≤ 30 сек;
  шторм одинаковых ошибок не спамит (тест на троттлинг).

### 1.2 Healthcheck и автоперезапуск (0.25 д)
- **Что:** `healthcheck` в docker-compose (curl `/api/health`), `restart` уже
  есть; для режима без webapp — лёгкий пинг getMe по таймеру.
- **DoD:** убитый uvicorn внутри контейнера приводит к рестарту контейнера.

### 1.3 Автобэкап данных (0.5 д)
- **Зачем:** `data/` (реестр xlsx, security.db, файлы счетов) — единственная
  база; потеря = потеря всех заявок.
- **Что:** ежедневный tar.gz каталога `data/` по cron (или встроенный
  JobQueue-таск бота) + отправка архива админу в Telegram (до 50 МБ) и/или в
  Google Drive при google-режиме; хранить N последних копий.
- **DoD:** архив приходит по расписанию; восстановление из архива описано в
  DEPLOY.md и проверено руками.

### 1.4 Внешний мониторинг доступности (0.25 д) ✅
- **Что:** бесплатный uptime-пинг `https://…/api/health` (UptimeRobot или
  cron с другого хоста); алерт при 5xx/таймауте — покрывает и смерть WARP.
- **DoD:** остановка контейнера даёт уведомление ≤ 5 мин. ✔ `/api/health`
  различает «умер контейнер» (таймаут/502) и «умер прокси» (503 при живом
  API); шаги настройки UptimeRobot — в DEPLOY.md.

### 1.5 План Б для прокси (0.25 д) ✅
- **Зачем:** WARP — внешняя зависимость с историей блокировок ТСПУ.
- **Что:** поддержка списка `PROXY_URL` через запятую с фейловером при
  таймауте getMe; документация «поднять ssh -D за 5 минут» уже есть.
- **DoD:** тест: недоступный первый прокси → бот стартует через второй.
  ✔ `tests/test_proxy_and_health.py::test_first_dead_second_wins`.

---

## Горизонт 2 · Уверенность в коде (~2–3 дня)

> **Статус:** 2.1 ✅, 2.2 ✅, 2.3 ✅, 2.4 ✅ — сделаны 2026-08-04.
> 144 python-тестов (интеграционные API через ASGI, e2e чат-диалога,
> Google-моки) + JS-тесты `webapp/form-lib.js` через `node --test` в CI.

103 юнит-теста покрывают чистые функции, но контрактные швы (HTTP, диалог,
Google) не проверяются — именно там живут регрессии.

### 2.1 Интеграционные тесты API (1 д)
- **Что:** httpx ASGI-клиент поверх `build_api()` с моком бота: полный
  `POST /api/invoice` (multipart с файлом, 200/401/403/409-дедуп/422/429),
  admin-эндпоинты, экспорт; фикстура подписанного initData уже есть.
- **DoD:** ≥ 15 сценариев, включая «дубль → force=1 → принято», в CI.

### 2.2 e2e чат-диалога (0.5 д)
- **Что:** прогон ConversationHandler по всем веткам (обычная/срочная/
  настраиваемая дата, своя статья, реквизиты, дубль) через сфабрикованные
  Update с моком bot API.
- **DoD:** каждая ветка доходит до `finalize_submission` с корректной заявкой.

### 2.3 Google-ветка на моках + живой smoke (0.5 д)
- **Что:** unit-тесты `google_backend` с замоканным `googleapiclient`
  (заголовки не трогаются, RAW/OVERWRITE, поиск строки по ID); опциональный
  живой smoke через `scripts/preflight.py` в CI по секретам (если заведём).
- **DoD:** регрессия вида «снова USER_ENTERED» ловится тестом.

### 2.4 Тесты JS-логики формы (0.5 д)
- **Что:** вынести чистые функции (parseAmount, checkRequisites, даты) в
  `webapp/form-lib.js`, подключить в index.html, гонять в CI через `node
  --test`; анти-дрейф-тесты уже сверяют константы.
- **DoD:** JS-кейсы сумм/ИНН/дат в CI; index.html не содержит дублей функций.

---

## Горизонт 3 · Архитектурная консолидация (~2–3 дня)

> **Статус:** 3.1 ✅ (SQLite — источник правды, xlsx — зеркало/экспорт,
> миграция `scripts/migrate_xlsx_to_sqlite.py`), 3.2 ✅ (карточки всех
> финансистов синхронизируются; «Отклонено/Отложено» запрашивают причину,
> автор получает её в уведомлении), 3.3 ✅ (PicklePersistence) —
> сделаны 2026-08-04. Остался 3.4 (рефакторинг разросшихся файлов).

### 3.1 SQLite как первичное хранилище заявок (1–1.5 д)
- **Зачем:** сейчас три механизма (xlsx + SQLite дедупа/аудита + Google);
  xlsx перечитывается целиком на каждую запись, консистентность зеркала —
  best-effort. SQLite даёт транзакции, выборки для отчётов и рост объёма.
- **Что:** таблица requests как источник правды локального режима; xlsx
  становится **экспортом** (генерация по запросу /export и по расписанию);
  фасад storage сохраняет интерфейс — хендлеры не меняются.
- **DoD:** все текущие тесты зелёные поверх нового бэкенда; /export отдаёт
  тот же красивый xlsx; миграционный скрипт переливает существующий xlsx.

### 3.2 Синхронизация карточек финансистов (0.5 д)
- **Зачем:** при нескольких финансистах чужие карточки не обновляются после
  смены статуса — можно «оплатить» уже отклонённое.
- **Что:** хранить (chat_id, message_id) разосланных карточек по заявке
  (в SQLite) и редактировать все при смене статуса.
- **DoD:** нажатие у одного финансиста обновляет карточки у всех.

### 3.3 Персистентность чат-диалогов (0.25 д)
- **Что:** `PicklePersistence` для ConversationHandler — рестарт контейнера
  не обрывает начатые формы.
- **DoD:** рестарт посреди шага «сумма» — пользователь продолжает с места.

### 3.4 Рефакторинг разросшихся модулей (0.5 д)
- **Что:** `bot/handlers.py` (~950 строк) → пакет (`form.py`, `menu.py`,
  `dedup_confirm.py`); JS из `index.html` → `form-lib.js` (см. 2.4).
- **DoD:** поведение не меняется (тесты зелёные), файлы ≤ ~400 строк.

---

## Горизонт 4 · Продуктовые фичи (по запросу бизнеса)

> **Статус:** сделаны «Мои заявки» (`/my` + экран 📋 в Mini App), повтор
> регулярной заявки, правка и отзыв заявки автором, удаление с аудитом,
> справочник контрагентов с реквизитами, панель финансиста (📊) с фильтрами
> и итогами, смена статуса прямо из панели и напоминания о сроках
> (2026-08-05). «Комментарий при отклонении» закрыт ещё в 3.2.

| Фича | Ценность | Оценка |
|---|---|---|
| **«Мои заявки»** — статусы своих заявок, суммы, даты, причина отказа ✅ | автор не теряет заявку из виду, даже если пуш не дошёл | 0.5 д |
| **Повтор заявки** — «↻ Повторить» подставляет контрагента, сумму, статью, реквизиты ✅ | регулярные платежи (аренда, хостинг) подаются в одно нажатие | 0.5 д |
| **Справочник контрагентов** — чипс подставляет имя вместе с реквизитами ✅ | меньше опечаток в платёжке | 0.5 д |
| **Отзыв заявки автором** — «🚫 Отозвать», пока статус «Новая» ✅ | ошибку в сумме не нужно «чинить» дублем | 0.25 д |
| **Комментарий при отклонении**: кнопка ❌ спрашивает причину, автор получает её ✅ | меньше пинг-понга в личках | 0.5 д |
| **Напоминания о сроках**: «завтра плановая дата» финансисту, просрочка — админу ✅ | заявки не протухают: `planned_date` наконец читается | 0.5 д |
| **Смена статуса из панели** — ✅/⏸/❌ с причиной, без перехода к карточке ✅ | финансист работает в одном окне | 0.5 д |
| **/report** — сводка за день/неделю/месяц: суммы по валютам и статьям, срочные, неоплаченные | финансист перестаёт считать руками | 0.5 д |
| **Ретеншн/архивация**: политика хранения файлов счетов и аудита (PII), авточистка | гигиена данных | 0.5 д |
| **README со скриншотами** сценария (9 экранов) для сдачи ТЗ | проверяющий видит результат без развёртывания | 0.25 д |
| Экспорт реестра за период (`/export 07.2026`) | бухгалтерии нужен месяц, а не всё | 0.25 д |

---

## Осознанно НЕ делаем (и почему)

- **Webhook вместо polling** — не решает исходящую блокировку api.telegram.org,
  добавляет требования к входящему контуру; polling через прокси работает.
- **Внешняя БД (Postgres) / очереди / микросервисы** — не тот масштаб:
  единицы заявок в день, один процесс достаточен (после 3.1 — тем более).
- **Горизонтальное масштабирование** — блокировки и кэши внутрипроцессные;
  осознанное ограничение «одна реплика», зафиксировано в CONTEXT.md.
- **Свой фронтенд-фреймворк для Mini App** — одна страница без сборки
  надёжнее для этого объёма UI.

---

## Сводная приоритизация

| # | Пункт | Усилие | Риск, который закрывает |
|---|---|---|---|
| 1 | 1.1 Алерты + 1.2 healthcheck | 0.75 д | тихая смерть бота |
| 2 | 1.3 Автобэкап | 0.5 д | потеря всех заявок |
| 3 | 2.1 Интеграционные тесты API | 1 д | регрессии на швах |
| 4 | 1.4 + 1.5 Uptime и прокси-фейловер | 0.5 д | зависимость от WARP |
| 5 | 2.2 + 2.3 e2e диалога и Google-моки | 1 д | слепые зоны тестов |
| 6 | 3.1 SQLite-первичное | 1.5 д | три хранилища, отчёты |
| 7 | 3.2 Синхронизация карточек | 0.5 д | конфликт статусов |
| 8 | Горизонт 4 по выбору | — | продуктовая ценность |

Итого до «спокойного продакшена» (горизонты 1–2): **~4 дня**. Горизонт 3 —
инвестиция перед ростом объёма или новыми фичами из горизонта 4.
