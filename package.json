{
  "name": "invoice-bot-webapp",
  "description": "Тесты Mini App: чистые функции формы и браузерные проверки",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --test tests/js/*.cjs",
    "test:e2e": "node --test --test-concurrency=1 tests/e2e/*.test.cjs",
    "brand": "node scripts/render_brand.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "private": true
}
